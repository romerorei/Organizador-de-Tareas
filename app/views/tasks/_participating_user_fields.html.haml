-#
    .nested-fields 
        = f.input :user_id, as: :select, collection: User.all - [current_user], label_method: -> (u) { u.email } 
        = f.input :role 
    -# role solo tendra 1: responsable y 2: seguidor
    -# f viene del _form.html.haml es la variable q le asiganmos
    -# user_id viene del modelo user y sera un selec en el formulario
    -# User.all - [current_user] se le resta el current_user para que no aparezca en el participantes
    -# xq el creador de la tarea no puede ser participante

.nested-fields
    %hr
    .text-right
    .row
        .col-4
            = f.input :user_id, label: 'Usuario', as: :select, collection: User.all - [current_user], label_method: -> (u) { u.email }, input_html: { class: 'selectize responsible' }
        .col-4
            = f.input :role, label: 'Rol', as: :select, collection: Participant.roles, value_method: -> (r) { r[1] }, input_html: { class: 'selectize role' }
        .col-4.text-left
            .mt-2
            = link_to_remove_association f, class: 'btn btn-warning btn-sm btn-flat mt-4' do
                .fas.fa-trash